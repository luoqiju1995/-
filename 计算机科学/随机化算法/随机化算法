knath 随机化算法
洗牌算法  shuffle

全排列 任选一个

长度为N的sequence -> 随机等概率置乱

i = N -> 1
swap(seq[i], seq(randrange[1, i]))
i = N-1 -> 0
swap(seq[i], seq(randrange[0, i]))

li = [1, 2, ..., i, ..., j, ..., N-1, N)
目的：使[1, N] 被选择的概率是 1/N

例：
一个元素想要被置于第N-1的位置：
在N交换的时候没有被选中，在N-1交换的时候被选中
(N-1)/N * 1/(N-1) = 1/N
一个元素想要被置于第N-2的位置：
在N交换的时候没有被选中，在N-1交换的时候没有被选中，在N-2交换的时候被选中
(N-1)/N * (N-2)/(N-1) * 1/(N-2) = 1/N

